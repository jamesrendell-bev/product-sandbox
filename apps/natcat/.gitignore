# dependencies
node_modules/

# build output
dist/
dist-ssr/

# env / secrets — never commit API keys
.env
.env.*
!.env.example

# editor / OS
.DS_Store
*.local
.vscode/*
!.vscode/extensions.json
.idea/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# NOTE: public/sample-bdx.xlsx is now a SYNTHETIC bordereau (500 fake AU rows)
# and IS shipped on purpose. Never place real coverholder data in public/.

# TypeScript incremental build info
*.tsbuildinfo
